<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Flashback Trading Cockpit</title>
  <style>
    :root{
      --bg: #ffffff;
      --text: #0f172a;
      --muted: #475569;
      --card: #ffffff;
      --border: #e2e8f0;
      --shadow: 0 1px 2px rgba(15, 23, 42, 0.06), 0 8px 24px rgba(15, 23, 42, 0.06);
      --good: #16a34a;
      --bad: #dc2626;
      --warn: #f59e0b;
      --info: #2563eb;
      --chip: #f1f5f9;
    }

    *{box-sizing:border-box}
    body{
      font-family: "Segoe UI", system-ui, -apple-system, Arial, sans-serif;
      background: var(--bg);
      color: var(--text);
      margin: 0;
      padding: 22px;
    }

    h1{
      margin: 0 0 18px 0;
      font-size: 20px;
      font-weight: 700;
      letter-spacing: .2px;
    }

    .subhead{
      margin: 0 0 18px 0;
      color: var(--muted);
      font-size: 13px;
    }

    .grid{
      display:grid;
      grid-template-columns:repeat(auto-fill, minmax(360px, 1fr));
      gap: 14px;
    }

    .card{
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 14px 14px 12px 14px;
      box-shadow: var(--shadow);
    }

    .header{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap: 10px;
      margin-bottom: 10px;
    }

    .acct{
      font-size: 16px;
      font-weight: 700;
    }

    .status{
      font-size: 12px;
      padding: 4px 10px;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: var(--chip);
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: .3px;
      font-weight: 600;
      white-space: nowrap;
    }

    /* Status variants (works with your current r.status string) */
    .RUNNING{ background: rgba(22,163,74,.10); border-color: rgba(22,163,74,.25); color: var(--good); }
    .STALE{ background: rgba(245,158,11,.10); border-color: rgba(245,158,11,.25); color: #92400e; }
    .DOWN{ background: rgba(220,38,38,.10); border-color: rgba(220,38,38,.25); color: var(--bad); }
    .DISABLED{ background: #f8fafc; border-color: var(--border); color: #64748b; }

    .rows{
      display:grid;
      gap: 6px;
      margin-top: 6px;
    }

    .metric{
      display:flex;
      justify-content:space-between;
      align-items:center;
      font-size: 13px;
      padding: 6px 0;
      border-bottom: 1px dashed #eef2f7;
    }

    .metric:last-child{
      border-bottom: none;
    }

    .k{ color: var(--muted); }
    .v{ font-weight: 650; color: var(--text); }

    .pos{ color: var(--good); }
    .neg{ color: var(--bad); }
    .muted{ color: var(--muted); }

    .bar{
      height: 8px;
      border-radius: 999px;
      background: #eef2f7;
      overflow:hidden;
      margin: 8px 0 6px 0;
      border: 1px solid var(--border);
    }

    .fill{
      height: 100%;
      width: 0%;
      background: var(--info);
    }

    .chips{
      display:flex;
      flex-wrap: wrap;
      gap: 6px;
      margin-top: 10px;
    }

    .chip{
      display:inline-flex;
      align-items:center;
      gap: 6px;
      padding: 5px 10px;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: var(--chip);
      font-size: 12px;
      color: var(--muted);
      font-weight: 600;
    }

    .footer{
      margin-top: 10px;
      font-size: 12px;
      color: var(--muted);
    }
  </style>
</head>

<body>
  <h1>Flashback Trading Cockpit</h1>
  <p class="subhead">Auto-refresh every 4s. (Yes, it’s brutal. Yes, it works.)</p>

  <div class="grid">
    {% for r in rows %}
    <div class="card">
      <div class="header">
        <div class="acct">{{ r.account }}</div>
        <div class="status {{ r.status }}">{{ r.status }}</div>
      </div>

      <!-- Main performance line -->
      <div class="rows">
        <div class="metric">
          <span class="k">PnL</span>
          <span class="v {% if r.pnl_pct >= 0 %}pos{% else %}neg{% endif %}">
            {{ r.pnl_pct }}%
          </span>
        </div>

        <div class="metric">
          <span class="k">Drawdown</span>
          <span class="v">{{ r.drawdown }}%</span>
        </div>
      </div>

      <!-- Visual bar -->
      <div class="bar">
        {# Clamp visual width to 0..100 (simple) #}
        {% set w = (r.pnl_pct + 20) * 2 %}
        {% if w < 0 %}{% set w = 0 %}{% endif %}
        {% if w > 100 %}{% set w = 100 %}{% endif %}
        <div class="fill" style="width: {{ w }}%"></div>
      </div>

      <div class="rows">
        <div class="metric"><span class="k">Confidence</span><span class="v">{{ r.confidence }}</span></div>
        <div class="metric"><span class="k">Win Rate</span><span class="v">{{ r.win_rate }}</span></div>
        <div class="metric"><span class="k">Trades</span><span class="v">{{ r.trades }}</span></div>
      </div>

      <div class="chips">
        <span class="chip">Risk: {{ r.risk }}</span>
        <span class="chip">Learning: {{ r.learning }}</span>
        <span class="chip">Intent: {{ r.intent }}</span>
      </div>

      <div class="footer">
        Health: {{ r.health }} | Locked: {{ r.locked }}
      </div>
    </div>
    {% endfor %}
  </div>

  <script>
    setTimeout(()=>location.reload(), 4000);
  </script>
</body>
</html>
